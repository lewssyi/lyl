# 运行test_preprocessing.py并保存输出

## 步骤
1. 运行tests/test_preprocessing.py文件，使用pytest执行测试
2. 使用重定向操作符将标准输出和标准错误保存到output.txt文件
3. 验证文件是否成功创建并包含测试结果

## 命令
```bash
python -m pytest tests/test_preprocessing.py -v > output.txt 2>&1
```

## 验证
执行完命令后，检查output.txt文件是否存在并包含测试结果。